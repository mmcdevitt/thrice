%section.center
  %h2 Your Purchase History
.table-wrap
  .table-wrap-header
    .clearfix
      .pull-right
        = link_to 'List new Item', sell_path, class: 'btn btn-info'
  %table.table
    %thead
      %tr
        %th Item
        %th Price
        %th Seller
        %th Date Purchased
    %tbody
      - @orders.each do |order|
        - order.order_items.all.each do |item|
          %tr
            %td= item.name
            %td= item.to_currency
            %td= item.seller_name
            %td= item.order_date